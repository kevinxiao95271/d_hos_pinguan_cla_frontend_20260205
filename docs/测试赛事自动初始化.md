# 测试赛事自动初始化功能

## 🎯 测试目标

验证赛事管理者登录后，系统能自动初始化当前赛事，无需手动切换。

---

## 📋 测试前准备

### 1. 启动本地开发服务器
```bash
npm run dev
```

### 2. 确保后端服务正常运行
- 后端地址：http://localhost:6031
- 确认有至少一个赛事数据

### 3. 准备测试账号
- 角色：赛事管理者（COMMITTEE_ADMIN）
- 手机号和密码

---

## 🧪 测试场景

### 场景1：首次登录（清空缓存）

**目的**：验证首次登录自动初始化赛事

**步骤**：
1. **清空浏览器缓存**
   - 按 F12 打开开发者工具
   - 进入 Application → Local Storage
   - 右键点击域名 → Clear
   - 或者直接清空所有 localStorage 项

2. **访问登录页**
   - 打开 http://localhost:5173/login
   - 确认 localStorage 为空

3. **登录**
   - 输入赛事管理者账号（手机号）
   - 输入密码
   - 点击"登录"

4. **观察控制台**
   - 按 F12 打开控制台
   - 应该看到：
     ```
     ⏳ 正在为赛事管理者初始化赛事...
     ✅ 自动设置当前赛事: 2025年浙江省品管大赛 (ID: 1)
     ```

5. **检查 localStorage**
   - Application → Local Storage
   - 确认有 `currentCompetitionId` 项
   - 值应该是一个赛事ID（如：1）

6. **验证页面正常**
   - 自动跳转到 `/committee/book-stage/registration`
   - 页面正常显示，无"赛事不存在"提示 ✅
   - 可以正常查看项目列表

**预期结果**：
- ✅ 自动初始化赛事
- ✅ 无需手动切换
- ✅ 页面正常显示

---

### 场景2：已有赛事ID（再次登录）

**目的**：验证已有赛事ID时不会重复初始化

**步骤**：
1. **保持登录状态**
   - localStorage 中已有 `currentCompetitionId`

2. **退出登录**
   - 点击右上角用户名 → 退出登录

3. **重新登录**
   - 输入账号密码登录

4. **观察控制台**
   - 应该看到：
     ```
     ⏳ 正在为赛事管理者初始化赛事...
     ✅ 已存在当前赛事ID: 1
     ```
   - 或者不会调用赛事列表API

5. **验证赛事ID未改变**
   - localStorage 中的 `currentCompetitionId` 保持不变

**预期结果**：
- ✅ 使用已有的赛事ID
- ✅ 不会重复初始化
- ✅ 页面正常显示

---

### 场景3：切换赛事

**目的**：验证手动切换赛事功能正常

**步骤**：
1. **进入切换赛事页面**
   - 左侧菜单点击"切换赛事"
   - 或访问 `/committee/switch-competition`

2. **查看赛事列表**
   - 应该显示所有赛事
   - 当前赛事的按钮显示"当前赛事"（禁用状态）

3. **切换到另一个赛事**
   - 点击其他赛事的"切换"按钮

4. **验证切换成功**
   - 提示：`已切换到赛事：XXX`
   - 自动跳转到报名统计页面
   - localStorage 中的 `currentCompetitionId` 更新

5. **刷新页面**
   - 按 F5 刷新
   - 赛事ID保持不变
   - 页面数据对应新赛事

**预期结果**：
- ✅ 可以手动切换赛事
- ✅ 切换后赛事ID正确更新
- ✅ 刷新后状态保持

---

### 场景4：退出登录清除赛事

**目的**：验证退出登录时清除赛事ID

**步骤**：
1. **登录并切换赛事**
   - 登录后切换到某个赛事
   - 确认 localStorage 中有 `currentCompetitionId`

2. **退出登录**
   - 点击右上角用户名 → 退出登录

3. **检查 localStorage**
   - Application → Local Storage
   - 确认 `currentCompetitionId` 已被删除 ✅
   - 其他登录信息（token、userInfo）也已清除

4. **重新登录**
   - 输入账号密码登录
   - 应该重新初始化赛事
   - 赛事ID可能与上次不同（自动选第一个）

**预期结果**：
- ✅ 退出时清除赛事ID
- ✅ 重新登录重新初始化
- ✅ 不会使用旧的赛事ID

---

### 场景5：暂无赛事数据

**目的**：验证无赛事时的处理

**步骤**：
1. **确保后端赛事列表为空**
   - 通过后端删除所有赛事
   - 或使用测试环境

2. **清空 localStorage 并登录**
   - 清空浏览器缓存
   - 使用赛事管理者账号登录

3. **观察控制台**
   - 应该看到：
     ```
     ⏳ 正在为赛事管理者初始化赛事...
     ⚠️ 暂无赛事数据
     ```

4. **验证页面行为**
   - 不会报错崩溃
   - localStorage 中无 `currentCompetitionId`
   - 页面可能提示"暂无赛事"

**预期结果**：
- ✅ 不会崩溃
- ✅ 记录警告日志
- ✅ 可以正常创建赛事

---

## ✅ 验收标准

### 必须通过的检查项

- [ ] 首次登录自动初始化赛事
- [ ] 控制台有正确的日志输出
- [ ] localStorage 中有 `currentCompetitionId`
- [ ] 页面不显示"赛事不存在"错误
- [ ] 已有赛事ID时不重复初始化
- [ ] 可以手动切换赛事
- [ ] 退出登录清除赛事ID
- [ ] 无赛事时不崩溃

### 代码检查

- [ ] 无 linter 错误
- [ ] 控制台无报错
- [ ] 网络请求正常

---

## 🐛 常见问题排查

### 问题1：登录后还是提示"赛事不存在"

**排查步骤**：
1. 检查控制台是否有错误
2. 检查网络请求是否成功（F12 → Network）
3. 检查 localStorage 是否有 `currentCompetitionId`
4. 检查后端赛事列表接口是否返回数据

### 问题2：控制台没有初始化日志

**排查步骤**：
1. 确认使用的是赛事管理者账号（COMMITTEE_ADMIN）
2. 检查 Login.vue 是否正确调用 `initializeCompetitionForAdmin`
3. 检查 `src/utils/competition.js` 是否存在

### 问题3：切换赛事后刷新又回到第一个赛事

**排查步骤**：
1. 检查切换时是否正确更新 localStorage
2. 检查是否有其他代码覆盖了 `currentCompetitionId`
3. 确认 `ensureCurrentCompetition` 检测到已有ID后直接返回

---

## 📊 测试报告模板

**测试人员**：______  
**测试时间**：______  
**测试环境**：本地开发环境

| 测试场景 | 预期结果 | 实际结果 | 是否通过 | 备注 |
|---------|---------|---------|---------|------|
| 场景1：首次登录 | 自动初始化赛事 | | ☐ 通过 ☐ 失败 | |
| 场景2：已有赛事ID | 不重复初始化 | | ☐ 通过 ☐ 失败 | |
| 场景3：切换赛事 | 正确切换和保持 | | ☐ 通过 ☐ 失败 | |
| 场景4：退出登录 | 清除赛事ID | | ☐ 通过 ☐ 失败 | |
| 场景5：暂无赛事 | 不崩溃 | | ☐ 通过 ☐ 失败 | |

**总体评价**：☐ 通过 ☐ 需要修复

**发现的问题**：
1. 
2. 

**建议**：
1. 
2. 

---

## 🎉 测试完成

如果所有场景都通过，恭喜！赛事自动初始化功能工作正常！

用户现在可以：
- ✅ 登录后直接使用，无需手动切换赛事
- ✅ 刷新页面保持赛事状态
- ✅ 需要时可以手动切换赛事
- ✅ 退出登录正确清理状态
