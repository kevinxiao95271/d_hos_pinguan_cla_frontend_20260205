# 分组前缀规则修正

## 📝 问题说明

**问题**: 自动分组时硬编码了 `A` 前缀，没有根据不同的竞赛组别使用对应的前缀。

**原因**: 竞赛组别在报名时已经确定，不同组别应该使用不同的分组前缀：
- **基层组 (BASIC)**: A 前缀 → A1, A2, A3...
- **综合组 (COMPREHENSIVE)**: B 前缀 → B1, B2, B3...
- **进阶组 (ADVANCED)**: C 前缀 → C1, C2, C3...

---

## 🔧 修改内容

### 1. 书审分组页面

**文件**: `src/views/committee/book/Registration.vue`

**修改位置**: `autoGroup()` 函数

#### 修改前
```javascript
const autoGroup = async () => {
  try {
    await ElMessageBox.confirm(
      '确定要自动分配分组吗？将按每组25人自动分配到A组系列（A1、A2、A3...）',
      '提示',
      {
        type: 'warning',
        confirmButtonText: '确定',
        cancelButtonText: '取消'
      }
    )
    
    const res = await autoGroupRegistrations({
      competitionId: filters.competitionId,
      groupPrefix: 'A',  // ❌ 硬编码 A 前缀
      groupSize: 25
    })
```

#### 修改后
```javascript
const autoGroup = async () => {
  try {
    // ✅ 检查是否选择了竞赛组别
    if (!filters.groupType) {
      ElMessage.warning('请先选择竞赛组别再进行自动分组')
      return
    }
    
    // ✅ 根据组别确定分组前缀
    const prefixMap = {
      'BASIC': 'A',           // 基层组
      'COMPREHENSIVE': 'B',   // 综合组
      'ADVANCED': 'C'         // 进阶组
    }
    const groupPrefix = prefixMap[filters.groupType]
    
    const groupTypeText = getGroupTypeText(filters.groupType)
    
    // ✅ 动态生成提示文字
    await ElMessageBox.confirm(
      `确定要对【${groupTypeText}】进行自动分组吗？将按每组25人自动分配到${groupPrefix}组系列（${groupPrefix}1、${groupPrefix}2、${groupPrefix}3...）`,
      '提示',
      {
        type: 'warning',
        confirmButtonText: '确定',
        cancelButtonText: '取消'
      }
    )
    
    const res = await autoGroupRegistrations({
      competitionId: filters.competitionId,
      groupType: filters.groupType,  // ✅ 指定组别
      groupPrefix: groupPrefix,      // ✅ 根据组别自动选择前缀
      groupSize: 25
    })
```

**关键变更**:
- ✅ 添加组别选择检查
- ✅ 根据 `filters.groupType` 动态确定前缀
- ✅ 提示文字动态显示组别名称和对应前缀
- ✅ API调用时传递 `groupType` 参数

---

### 2. 赛事详情页面

**文件**: `src/views/committee/CompetitionDetail.vue`

**修改位置**: `autoGroup()` 函数

#### 修改前
```javascript
const autoGroup = async () => {
  try {
    await autoGroupRegistrations({
      competitionId: competitionId.value,
      groupPrefix: 'A',  // ❌ 硬编码 A 前缀
      groupSize: 25
    })
    ElMessage.success('自动分组成功')
    loadRegistrations()
  } catch (error) {
    console.error('自动分组失败:', error)
  }
}
```

#### 修改后
```javascript
const autoGroup = async () => {
  try {
    // ✅ 检查是否选择了竞赛组别
    if (!registrationFilters.groupType) {
      ElMessage.warning('请先选择竞赛组别再进行自动分组')
      return
    }
    
    // ✅ 根据组别确定分组前缀
    const prefixMap = {
      'BASIC': 'A',           // 基层组
      'COMPREHENSIVE': 'B',   // 综合组
      'ADVANCED': 'C'         // 进阶组
    }
    const groupPrefix = prefixMap[registrationFilters.groupType]
    const groupTypeText = getGroupTypeText(registrationFilters.groupType)
    
    // ✅ 添加确认对话框
    await ElMessageBox.confirm(
      `确定要对【${groupTypeText}】进行自动分组吗？将按每组25人自动分配到${groupPrefix}组系列（${groupPrefix}1、${groupPrefix}2、${groupPrefix}3...）`,
      '提示',
      {
        type: 'warning',
        confirmButtonText: '确定',
        cancelButtonText: '取消'
      }
    )
    
    await autoGroupRegistrations({
      competitionId: competitionId.value,
      groupType: registrationFilters.groupType,  // ✅ 指定组别
      groupPrefix: groupPrefix,                  // ✅ 根据组别自动选择前缀
      groupSize: 25
    })
    
    ElMessage.success('自动分组成功')
    loadRegistrations()
  } catch (error) {
    if (error !== 'cancel') {
      console.error('自动分组失败:', error)
      ElMessage.error('自动分组失败')
    }
  }
}
```

**关键变更**:
- ✅ 添加组别选择检查
- ✅ 根据 `registrationFilters.groupType` 动态确定前缀
- ✅ 添加确认对话框
- ✅ API调用时传递 `groupType` 参数
- ✅ 添加 `ElMessageBox` 导入

---

### 3. 面谈分组页面

**文件**: `src/views/committee/interview/Group.vue`

**状态**: ✅ 无需修改

**原因**: 面谈分组页面已经正确使用了 C 前缀，因为面谈阶段只针对进阶组。

**代码**（第584行）:
```javascript
const groupCode = `C${i + 1}`  // ✅ 正确使用 C 前缀
```

---

## 📊 修改总览

| 页面 | 文件 | 修改内容 | 状态 |
|------|------|---------|------|
| 书审分组 | `Registration.vue` | 添加组别检查，动态确定前缀 | ✅ 已修改 |
| 赛事详情 | `CompetitionDetail.vue` | 添加组别检查，动态确定前缀，添加确认框 | ✅ 已修改 |
| 面谈分组 | `Group.vue` | 已正确使用C前缀 | ✅ 无需修改 |

---

## 🎯 分组前缀规则

### 完整的前缀映射

```javascript
const prefixMap = {
  'BASIC': 'A',           // 基层组 → A1, A2, A3, A4...
  'COMPREHENSIVE': 'B',   // 综合组 → B1, B2, B3, B4...
  'ADVANCED': 'C'         // 进阶组 → C1, C2, C3, C4...
}
```

### 分组示例

#### 基层组（50个项目）
- 筛选条件：竞赛组别 = 基层组
- 自动分组：按每组25人
- 分组结果：A1（25人）、A2（25人）

#### 综合组（75个项目）
- 筛选条件：竞赛组别 = 综合组
- 自动分组：按每组25人
- 分组结果：B1（25人）、B2（25人）、B3（25人）

#### 进阶组（40个项目）
- 筛选条件：竞赛组别 = 进阶组
- 自动分组：按每组25人
- 分组结果：C1（25人）、C2（15人）

---

## ⚠️ 重要说明

### 1. 竞赛组别在报名时确定
- 组别由报名人在报名时选择
- **组别一经确定，不可修改**
- 自动分组只能在同一组别内进行

### 2. 必须先选择组别
- 自动分组前**必须**先筛选竞赛组别
- 如果未选择组别，系统会提示：
  > ⚠️ 请先选择竞赛组别再进行自动分组

### 3. 不同组别使用不同前缀
- 基层组 → A 前缀
- 综合组 → B 前缀
- 进阶组 → C 前缀
- **不能混用前缀**

### 4. 分组编号从1开始
- 每个组别的分组编号独立计数
- 例如：A1, A2, A3... 和 B1, B2, B3... 是独立的

---

## 🧪 测试建议

### 测试场景1: 基层组自动分组
1. 进入书审分组页面
2. 筛选条件选择：竞赛组别 = 基层组
3. 点击"自动分组"
4. **预期提示**: "确定要对【基层组】进行自动分组吗？将按每组25人自动分配到A组系列（A1、A2、A3...）"
5. 确认后分组结果应该是 A1, A2, A3...

### 测试场景2: 综合组自动分组
1. 进入书审分组页面
2. 筛选条件选择：竞赛组别 = 综合组
3. 点击"自动分组"
4. **预期提示**: "确定要对【综合组】进行自动分组吗？将按每组25人自动分配到B组系列（B1、B2、B3...）"
5. 确认后分组结果应该是 B1, B2, B3...

### 测试场景3: 进阶组自动分组
1. 进入书审分组页面
2. 筛选条件选择：竞赛组别 = 进阶组
3. 点击"自动分组"
4. **预期提示**: "确定要对【进阶组】进行自动分组吗？将按每组25人自动分配到C组系列（C1、C2、C3...）"
5. 确认后分组结果应该是 C1, C2, C3...

### 测试场景4: 未选择组别
1. 进入书审分组页面
2. 不选择竞赛组别（或选择"全部"）
3. 点击"自动分组"
4. **预期提示**: "⚠️ 请先选择竞赛组别再进行自动分组"

### 测试场景5: 面谈分组（进阶组）
1. 进入面谈分组页面
2. 点击"自动分组"
3. 输入每组人数（默认25）
4. 确认后分组结果应该是 C1, C2, C3...（只有进阶组）

---

## 📋 后端API说明

### 自动分组API

**接口**: `POST /api/admin/registrations/auto-group`

**请求参数**（修改后）:
```json
{
  "competitionId": 1,
  "groupType": "BASIC",      // ✅ 新增：指定组别
  "groupPrefix": "A",        // ✅ 根据组别动态设置
  "groupSize": 25
}
```

**参数说明**:
- `competitionId`: 赛事ID
- `groupType`: 竞赛组别（BASIC/COMPREHENSIVE/ADVANCED）
- `groupPrefix`: 分组前缀（A/B/C）
- `groupSize`: 每组人数（固定25）

**后端需要确保**:
- 只对指定 `groupType` 的项目进行分组
- 使用正确的 `groupPrefix` 生成分组编号
- 不会跨组别分组

---

## 📚 代码中已存在的分组规则

### 书审分组页面

**可用分组代码**（第536-548行）:
```javascript
const availableGroupCodes = computed(() => {
  const groupType = changeGroupForm.groupType
  if (!groupType) return []
  
  if (groupType === 'BASIC') {
    return ['A1', 'A2', 'A3', 'A4', 'A5', 'A6', 'A7', 'A8', 'A9', 'A10']
  } else if (groupType === 'COMPREHENSIVE') {
    return ['B1', 'B2', 'B3', 'B4', 'B5', 'B6', 'B7', 'B8', 'B9', 'B10']
  } else if (groupType === 'ADVANCED') {
    return ['C1', 'C2', 'C3', 'C4', 'C5', 'C6', 'C7', 'C8', 'C9', 'C10']
  }
  return []
})
```

### 面谈分组页面

**进阶组分组代码**（第384行）:
```javascript
const advancedGroupCodes = ['C1', 'C2', 'C3', 'C4', 'C5', 'C6', 'C7', 'C8', 'C9', 'C10']
```

---

## ✅ 修改完成

- ✅ 书审分组：根据组别动态选择前缀（A/B/C）
- ✅ 赛事详情：根据组别动态选择前缀（A/B/C）
- ✅ 面谈分组：已正确使用C前缀（无需修改）
- ✅ 添加组别选择检查
- ✅ 动态生成提示文字
- ✅ 无linter错误
- ✅ 符合业务规则

---

## 💡 后续建议

### 1. 后端验证
建议后端在自动分组时也验证：
- `groupType` 和 `groupPrefix` 是否匹配
- 不会跨组别分组

### 2. 用户体验优化
可以在页面上更明显地提示：
- 不同组别对应不同的分组前缀
- 组别在报名时确定，不可修改

### 3. 批量操作提示
批量分组时，系统已经检查了组别一致性：
```javascript
const allSameGroupType = selectedRegistrations.value.every(r => r.groupType === firstGroupType)
if (!allSameGroupType) {
  ElMessage.error('批量分类只能在同一竞赛组别内进行。请选择同一组别的项目。')
  return
}
```

---

**修改完成时间**: 2026-02-26  
**修改人**: AI助手  
**测试状态**: 待测试  
**业务规则**: 符合组别前缀规则（A=基层组，B=综合组，C=进阶组）
