# 前端部署步骤（快速指南）

## 📦 已准备好的文件
- ✅ `dist/` - 构建产物已生成
- ✅ `deploy.sh` - Linux/Mac 部署脚本
- ✅ `upload.bat` - Windows 上传脚本
- ✅ `docs/服务器部署指南.md` - 详细部署文档

---

## 🚀 快速部署（3步）

### 方式A: 使用命令行（推荐）

#### 步骤1: 上传文件
在项目根目录打开PowerShell或CMD，执行：
```cmd
scp -r dist/* root@81.71.44.180:/data/pinguan_frontend/
```
输入密码: `Yiguo9527_`

#### 步骤2: 登录服务器
```bash
ssh root@81.71.44.180
# 密码: Yiguo9527_
```

#### 步骤3: 配置nginx并启动
```bash
# 创建nginx配置
cat > /etc/nginx/conf.d/pinguan.conf << 'EOF'
server {
    listen 6039;
    server_name _;
    root /data/pinguan_frontend;
    index index.html;
    
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    location /api/ {
        proxy_pass http://localhost:6031/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
EOF

# 测试并重启nginx
nginx -t
systemctl restart nginx
systemctl status nginx
```

### 方式B: 使用WinSCP图形界面（最简单）

1. 下载WinSCP: https://winscp.net/
2. 连接信息:
   - 主机: 81.71.44.180
   - 用户: root
   - 密码: Yiguo9527_
3. 导航到 `/data/pinguan_frontend`
4. 上传 `dist` 目录下的所有文件
5. 按照步骤3配置nginx

---

## ✅ 验证部署

访问: http://81.71.44.180:6039

应该能看到登录页面

---

## 📝 注意事项

1. **不要上传源码**：只上传dist目录的构建产物
2. **不要在服务器编译**：所有编译在本地完成
3. **前端端口6039**：浏览器访问前端页面的端口
4. **后端端口6031**：API代理到后端服务

---

## 🔧 如果需要重新部署

```bash
# 1. 本地重新构建
npm run build

# 2. 清空服务器旧文件
ssh root@81.71.44.180
rm -rf /data/pinguan_frontend/*

# 3. 重新上传
scp -r dist/* root@81.71.44.180:/data/pinguan_frontend/

# 4. 重启nginx
nginx -s reload
```
