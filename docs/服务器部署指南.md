# 浙江省品管大赛前端 - 服务器部署指南

## 服务器信息
- **IP地址**: 81.71.44.180
- **用户**: root
- **密码**: Yiguo9527_
- **部署目录**: /data/pinguan_frontend
- **端口**: 6039

---

## 方式一：使用PowerShell脚本（推荐）

### 1. 确保已构建
```powershell
npm run build
```

### 2. 上传文件到服务器
```powershell
# 使用scp上传（需要输入密码: Yiguo9527_）
scp -r dist/* root@81.71.44.180:/data/pinguan_frontend/
```

### 3. SSH登录服务器配置nginx
```bash
ssh root@81.71.44.180
# 输入密码: Yiguo9527_
```

### 4. 在服务器上创建nginx配置
```bash
cat > /etc/nginx/conf.d/pinguan.conf << 'EOF'
server {
    listen 6039;
    server_name _;
    
    root /data/pinguan_frontend;
    index index.html;
    
    # 启用gzip压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
    
    # 前端路由支持
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # API代理到后端
    location /api/ {
        proxy_pass http://localhost:6031/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
EOF
```

### 5. 测试并重启nginx
```bash
# 测试配置
nginx -t

# 重启nginx
systemctl restart nginx

# 查看nginx状态
systemctl status nginx
```

### 6. 验证部署
浏览器访问: http://81.71.44.180:6039

---

## 方式二：使用WinSCP图形界面（最简单）

### 1. 下载WinSCP
https://winscp.net/

### 2. 连接服务器
- 文件协议: SFTP
- 主机名: 81.71.44.180
- 端口号: 22
- 用户名: root
- 密码: Yiguo9527_

### 3. 上传文件
- 在右侧服务器窗口导航到 `/data/pinguan_frontend`
- 在左侧本地窗口导航到项目的 `dist` 目录
- 选中dist目录下的所有文件，拖拽到右侧服务器窗口

### 4. 配置nginx（参考方式一的步骤4-5）

---

## 方式三：手动命令行部署

### 1. 压缩dist目录
```powershell
# 在项目根目录
Compress-Archive -Path dist\* -DestinationPath dist.zip -Force
```

### 2. 上传zip文件
```powershell
scp dist.zip root@81.71.44.180:/data/
# 输入密码: Yiguo9527_
```

### 3. SSH登录服务器
```bash
ssh root@81.71.44.180
# 输入密码: Yiguo9527_
```

### 4. 解压并部署
```bash
# 创建目录
mkdir -p /data/pinguan_frontend

# 安装unzip（如果没有）
yum install -y unzip

# 解压文件
cd /data
unzip -o dist.zip -d pinguan_frontend/

# 查看文件
ls -la /data/pinguan_frontend/
```

### 5. 配置nginx（参考方式一的步骤4-5）

---

## 重要配置说明

### Nginx配置要点
1. **端口**: 保持6039不变
2. **根目录**: /data/pinguan_frontend
3. **API代理**: /api/ 代理到 http://localhost:8080/api/
4. **前端路由**: 使用 try_files 支持 Vue Router 的 history 模式

### 验证清单
- [ ] 访问 http://81.71.44.180:6039 能看到登录页
- [ ] 登录功能正常（API代理工作）
- [ ] 刷新页面不会404（前端路由支持）
- [ ] 静态资源正常加载（JS、CSS、图片）

---

## 故障排查

### 问题1: 页面无法访问
```bash
# 检查nginx状态
systemctl status nginx

# 查看nginx错误日志
tail -f /var/log/nginx/error.log

# 检查端口占用
netstat -tlnp | grep 6039
```

### 问题2: API请求失败
```bash
# 检查后端服务是否运行
netstat -tlnp | grep 6031

# 查看nginx访问日志
tail -f /var/log/nginx/access.log
```

### 问题3: 前端路由404
检查nginx配置中的 `try_files $uri $uri/ /index.html;` 是否正确配置

---

## 快速命令参考

```bash
# 查看已部署的文件
ls -lh /data/pinguan_frontend/

# 重新加载nginx配置（不中断服务）
nginx -s reload

# 查看nginx配置
cat /etc/nginx/conf.d/pinguan.conf

# 清理旧文件重新部署
rm -rf /data/pinguan_frontend/*
# 然后重新上传
```
