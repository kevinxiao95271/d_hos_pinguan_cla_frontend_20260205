# 报名时间字段说明

## 📍 字段来源

### 1️⃣ 书审分组列表

**文件**: `src/views/committee/book/Registration.vue`

**表格列定义** (第171-175行):
```vue
<el-table-column prop="submittedAt" label="报名时间" width="170">
  <template #default="{ row }">
    {{ formatDate(row.submittedAt) }}
  </template>
</el-table-column>
```

**数据来源**:
- 函数：`loadRegistrations()` (第646-668行)
- API调用：`filterRegistrations(params)` (第649行)
- API定义文件：`src/api/admin.js` (第50-56行)

**API详情**:
```javascript
export function filterRegistrations(params) {
  return request({
    url: '/admin/registrations/filter',
    method: 'get',
    params
  })
}
```

**字段路径**: `row.submittedAt`

---

### 2️⃣ 面谈分组列表

**文件**: `src/views/committee/interview/Group.vue`

#### 2.1 列表数据

**数据来源**:
- 函数：`loadPoolData()` (第450-472行)
- API调用：`filterRegistrations(params)` (第453行)
- API定义：同书审分组，`GET /admin/registrations/filter`

**字段路径**: `row.submittedAt` (虽然表格中没直接显示列，但数据中包含此字段)

#### 2.2 详情对话框

**详情显示** (第222-224行):
```vue
<el-descriptions-item label="报名时间">
  {{ formatDate(currentDetail.registration.submittedAt) }}
</el-descriptions-item>
```

**数据来源**:
- 函数：`viewDetail(row)` (第498-508行)
- API调用：`getRegistration(row.registrationId)` (第503行)
- API定义文件：`src/api/registration.js` (第129-134行)

**API详情**:
```javascript
export function getRegistration(id) {
  return request({
    url: `/registrations/${id}`,
    method: 'get'
  })
}
```

**字段路径**: `currentDetail.registration.submittedAt`

---

## 🎯 总结

### API总览

| 页面 | 数据类型 | API地址 | HTTP方法 | 字段路径 |
|------|---------|---------|---------|---------|
| 书审分组 | 列表 | `/admin/registrations/filter` | GET | `row.submittedAt` |
| 书审分组 | 详情 | `/registrations/{id}` | GET | `registration.submittedAt` |
| 面谈分组 | 列表 | `/admin/registrations/filter` | GET | `row.submittedAt` |
| 面谈分组 | 详情 | `/registrations/{id}` | GET | `registration.submittedAt` |

### 字段名称

**统一字段名**: `submittedAt`

**含义**: 报名提交时间

**数据类型**: 日期时间字符串 (ISO 8601格式)

**前端格式化**: 
```javascript
const formatDate = (date) => {
  return date ? dayjs(date).format('YYYY-MM-DD HH:mm:ss') : '-'
}
```

**显示格式**: `YYYY-MM-DD HH:mm:ss`  
**示例**: `2026-02-25 14:30:15`

---

## 📦 后端API响应结构

### 列表API: `GET /admin/registrations/filter`

**响应格式** (推测):
```json
{
  "success": true,
  "data": {
    "content": [
      {
        "registrationId": 1001,
        "projectName": "项目名称",
        "institutionName": "机构名称",
        "submittedAt": "2026-02-25T14:30:15",  // ← 报名时间字段
        "applicantName": "申请人",
        "groupCode": "A01",
        "methodLabel": "品管工具",
        // ...其他字段
      }
    ],
    "totalElements": 100,
    "totalPages": 10
  }
}
```

### 详情API: `GET /api/registrations/{id}`

**响应格式** (推测):
```json
{
  "success": true,
  "data": {
    "registration": {
      "id": 1001,
      "projectName": "项目名称",
      "submittedAt": "2026-02-25T14:30:15",  // ← 报名时间字段
      "groupCode": "A01",
      "status": "APPROVED",
      // ...其他字段
    },
    "activityInfo": {
      // 活动信息
    },
    "summary": {
      // 项目总结
    }
  }
}
```

---

## 🔧 如何验证API返回数据

### 方法1：浏览器开发者工具

1. 打开书审分组或面谈分组页面
2. 按 F12 打开开发者工具
3. 切换到 Network 标签
4. 刷新页面或点击查询
5. 找到以下请求：
   - 列表：`/admin/registrations/filter`
   - 详情：`/registrations/{id}`
6. 查看 Response，找到 `submittedAt` 字段

### 方法2：查看控制台日志

代码中有日志输出：
```javascript
console.log(`✅ 加载到 ${registrations.value.length} 条报名数据`)
```

可以在控制台查看数据结构。

### 方法3：使用Postman或Swagger

- 访问后端的 Swagger 文档
- 找到对应的接口
- 执行并查看返回的 `submittedAt` 字段

---

## 💡 注意事项

### 1. 字段名称一致性

- 列表和详情使用的都是 `submittedAt` 字段
- 前端没有对字段名进行任何转换

### 2. 日期格式

- 后端返回：ISO 8601 格式 (如 `2026-02-25T14:30:15`)
- 前端显示：`YYYY-MM-DD HH:mm:ss` (如 `2026-02-25 14:30:15`)

### 3. 空值处理

```javascript
{{ formatDate(row.submittedAt) }}
// 如果 submittedAt 为 null 或 undefined，显示 '-'
```

### 4. 两个页面共用API

书审分组和面谈分组的列表数据都是调用同一个API：
- `GET /admin/registrations/filter`
- 通过不同的 `params` 参数来筛选不同阶段的数据

---

## 📋 给后端的信息

如果需要修改或调试报名时间字段，后端需要确认：

1. **列表接口**: `GET /admin/registrations/filter`
   - 返回数组中的每个对象是否包含 `submittedAt` 字段
   - 字段格式是否为 ISO 8601 日期时间字符串

2. **详情接口**: `GET /api/registrations/{id}`
   - 返回对象的 `registration` 属性中是否包含 `submittedAt` 字段
   - 字段格式是否为 ISO 8601 日期时间字符串

3. **字段值**:
   - `submittedAt` 应该是报名提交的时间戳
   - 不应该为空（除非报名还未提交）
   - 应该早于或等于当前时间

---

**文档生成时间**: 2026-02-26  
**目的**: 说明书审分组和面谈分组列表中"报名时间"字段的数据来源
