# 🔍 赛事列表重复报名问题诊断

## 📋 问题描述

用户指出的逻辑矛盾：

1. ✅ "我的报名"页面显示用户已经报名了"门诊预约体验提升-11"项目
2. ❌ "赛事列表"页面所有赛事都显示"立即报名"按钮，包括已报名的赛事
3. **矛盾点**：既然能查到报名记录，为什么不能判断哪些赛事已报名？

---

## 🔎 问题根源

### API调查结果

#### 1. 我的报名接口

**接口**: `GET /api/registrations/my`

**实际返回数据**:
```json
{
  "success": true,
  "data": [
    {
      "id": 116,
      "projectName": "门诊预约体验提升-11",
      "groupType": "COMPREHENSIVE",
      "status": "APPROVED",
      "competitionId": null,  // ← 问题所在！
      "institutionId": null,
      "createdAt": "2026-02-05T23:38:52.604359",
      "submittedAt": "2026-02-05T01:38:52.604359"
    }
  ]
}
```

**关键发现**:
- ✅ 接口**有** `competitionId` 字段
- ❌ 字段值是 `null` (空值)

---

## ⚠️ 问题分析

### 为什么会导致重复报名按钮？

```
用户已报名赛事 (ID未知)
           ↓
     competitionId = null
           ↓
    前端无法判断已报名哪个赛事
           ↓
   所有赛事都显示"立即报名"
           ↓
    用户可能重复报名同一赛事！❌
```

### 正确的逻辑应该是

```
用户报名 → 保存 competitionId = 21
           ↓
GET /api/registrations/my
           ↓
data = [{..., competitionId: 21, ...}]
           ↓
前端判断：赛事21已报名
           ↓
赛事列表中ID=21的显示"已报名"或"查看报名"
           ↓
避免重复报名！✅
```

---

## 🔧 后端需要修复

### 问题定位

**数据库表**: `registration` 表

**问题字段**: `competition_id` 列

**当前状态**: 该字段为 `NULL`

### 修复方案1: 更新现有数据

**如果报名时就知道赛事ID**（推荐）:

```sql
-- 查看当前报名记录的赛事关联情况
SELECT id, project_name, competition_id, created_at 
FROM registration 
WHERE applicant_id = (SELECT id FROM user WHERE phone = '13966000011');

-- 如果能确定报名记录对应的赛事，更新它
-- 例如：报名ID=116 对应赛事ID=21
UPDATE registration 
SET competition_id = 21 
WHERE id = 116;
```

### 修复方案2: 修复报名创建逻辑

**检查报名创建接口**: `POST /api/registrations`

**确保创建时保存赛事ID**:

```java
// 报名时应该从请求中获取 competitionId
@PostMapping("/registrations")
public ResponseEntity<Registration> createRegistration(
    @RequestBody RegistrationRequest request
) {
    Registration registration = new Registration();
    registration.setCompetitionId(request.getCompetitionId());  // ← 必须设置！
    registration.setProjectName(request.getProjectName());
    registration.setGroupType(request.getGroupType());
    // ... 其他字段
    
    return registrationService.save(registration);
}
```

### 修复方案3: 从其他关联表推断

**如果报名通过其他方式关联赛事**:

```sql
-- 方案A: 通过时间推断（不推荐，不准确）
UPDATE registration r
SET competition_id = (
    SELECT c.id 
    FROM competition c 
    WHERE r.created_at BETWEEN c.register_start AND c.register_end
    LIMIT 1
)
WHERE r.competition_id IS NULL;

-- 方案B: 如果有其他关联表
-- 例如通过 review_task 表推断
UPDATE registration r
SET competition_id = (
    SELECT rt.competition_id 
    FROM review_task rt 
    WHERE rt.registration_id = r.id 
    LIMIT 1
)
WHERE r.competition_id IS NULL;
```

---

## 📊 数据验证

### 验证脚本

**检查报名记录是否有赛事ID**:

```sql
-- 1. 检查所有报名的赛事ID情况
SELECT 
    id,
    project_name,
    competition_id,
    CASE 
        WHEN competition_id IS NULL THEN '缺失'
        ELSE '正常'
    END as status
FROM registration;

-- 2. 统计缺失情况
SELECT 
    COUNT(*) as total,
    SUM(CASE WHEN competition_id IS NULL THEN 1 ELSE 0 END) as missing_count,
    SUM(CASE WHEN competition_id IS NOT NULL THEN 1 ELSE 0 END) as has_id_count
FROM registration;
```

### Python测试脚本

**文件**: `scripts/test_my_registrations_structure.py`

```bash
python scripts/test_my_registrations_structure.py
```

**期望输出**:
```
赛事ID (competitionId): 21  ← 应该有值！
```

---

## 🎯 前端处理方案

### 方案A: 等待后端修复（推荐）✅

**优点**: 
- 数据准确
- 避免重复报名
- 符合业务逻辑

**后端修复后，前端需要改动**:

```vue
<!-- src/views/contestant/Competitions.vue -->

<script setup>
import { getCompetitions } from '@/api/competition'
import { getMyRegistrations } from '@/api/registration'

const competitions = ref([])
const myRegistrations = ref([])

// 1. 同时加载赛事列表和我的报名
const loadData = async () => {
  try {
    loading.value = true
    
    // 并行加载
    const [competitionsRes, myRegsRes] = await Promise.all([
      getCompetitions(),
      getMyRegistrations()
    ])
    
    if (competitionsRes.success) {
      competitions.value = competitionsRes.data || []
    }
    
    if (myRegsRes.success) {
      myRegistrations.value = myRegsRes.data || []
    }
  } finally {
    loading.value = false
  }
}

// 2. 判断赛事是否已报名
const isRegistered = (competitionId) => {
  return myRegistrations.value.some(
    reg => reg.competitionId === competitionId
  )
}

// 3. 获取已报名的记录
const getMyRegistration = (competitionId) => {
  return myRegistrations.value.find(
    reg => reg.competitionId === competitionId
  )
}

// 4. 修改按钮逻辑
const canRegister = (item) => {
  // 只有报名阶段且未报名的才能报名
  return item.stage === 'REGISTER' && !isRegistered(item.id)
}

const getButtonText = (item) => {
  if (isRegistered(item.id)) {
    return '查看报名'
  }
  return canRegister(item) ? '立即报名' : '报名已结束'
}

const handleButtonClick = (item) => {
  if (isRegistered(item.id)) {
    // 跳转到报名详情
    const myReg = getMyRegistration(item.id)
    if (myReg) {
      router.push(`/contestant/registration/${myReg.id}`)
    }
  } else {
    // 跳转到报名页面
    router.push(`/contestant/register/${item.id}`)
  }
}
</script>

<template>
  <el-button
    :type="isRegistered(item.id) ? 'default' : 'primary'"
    :disabled="!canRegister(item.id) && !isRegistered(item.id)"
    @click="handleButtonClick(item)"
  >
    {{ getButtonText(item) }}
  </el-button>
</template>
```

### 方案B: 临时前端解决（不推荐）

**通过项目名称模糊匹配**（不准确）:

```javascript
// 不推荐：依赖项目名称
const isRegistered = (competitionName) => {
  return myRegistrations.value.some(
    reg => reg.projectName.includes(competitionName)
  )
}
```

**缺点**:
- 不准确
- 项目名称可能重复
- 无法处理重名赛事

---

## 📝 给后端的修复建议

### 紧急修复

**步骤1**: 找出报名ID=116对应的赛事

```sql
-- 方法1: 查看报名时间，推测对应的赛事
SELECT r.id, r.project_name, r.created_at, c.id as comp_id, c.name
FROM registration r
CROSS JOIN competition c
WHERE r.id = 116
  AND r.created_at BETWEEN c.register_start AND c.register_end;

-- 方法2: 通过评审任务关联
SELECT r.id, r.project_name, rt.competition_id, c.name
FROM registration r
LEFT JOIN review_task rt ON rt.registration_id = r.id
LEFT JOIN competition c ON c.id = rt.competition_id
WHERE r.id = 116;
```

**步骤2**: 更新报名记录

```sql
-- 假设确定报名116对应赛事21
UPDATE registration 
SET competition_id = 21 
WHERE id = 116;
```

**步骤3**: 验证

```bash
python scripts/test_my_registrations_structure.py
```

期望看到:
```
赛事ID (competitionId): 21  ✅
```

### 长期修复

**修改报名创建接口**，确保创建时就保存 `competition_id`:

```java
@PostMapping("/registrations")
public ResponseEntity<?> createRegistration(
    @RequestBody RegistrationRequest request,
    @AuthenticationPrincipal UserDetails userDetails
) {
    // 验证参数
    if (request.getCompetitionId() == null) {
        return ResponseEntity.badRequest()
            .body("赛事ID不能为空");  // ← 强制要求
    }
    
    // 创建报名
    Registration registration = new Registration();
    registration.setCompetitionId(request.getCompetitionId());  // ← 必须设置
    registration.setApplicantId(getUserId(userDetails));
    registration.setProjectName(request.getProjectName());
    registration.setGroupType(request.getGroupType());
    
    return ResponseEntity.ok(registrationService.save(registration));
}
```

---

## ✅ 完整修复清单

### 后端必须修复 ✅

| 项目 | 状态 | 说明 |
|-----|------|------|
| 更新现有报名记录 | ⏳ 待修复 | `UPDATE registration SET competition_id = ? WHERE id = 116` |
| 修改创建报名逻辑 | ⏳ 待修复 | 报名时必须保存 `competitionId` |
| 添加数据验证 | ⏳ 待修复 | `competitionId` 不能为空 |

### 前端待修改 ⏳

| 项目 | 状态 | 依赖 |
|-----|------|------|
| 赛事列表页面 | ⏳ 待修改 | 需要后端先修复数据 |
| 加载我的报名 | ⏳ 待修改 | 判断已报名赛事 |
| 按钮状态控制 | ⏳ 待修改 | 已报名显示"查看报名" |

---

## 🎯 总结

### 问题根源

**后端数据缺失**: 报名记录的 `competition_id` 字段为 `NULL`

### 影响

- ❌ 前端无法判断用户已报名哪些赛事
- ❌ 所有赛事都显示"立即报名"
- ❌ 可能导致用户重复报名同一赛事

### 解决方案

1. **后端**: 更新现有数据 + 修改创建逻辑
2. **前端**: 加载我的报名 + 判断已报名状态

### 优先级

**🔴 高优先级** - 这是业务逻辑问题，必须尽快修复！

---

**诊断完成时间**: 2026-02-06

**下一步**: 等待后端修复 `competition_id` 数据，然后前端实现判断逻辑
