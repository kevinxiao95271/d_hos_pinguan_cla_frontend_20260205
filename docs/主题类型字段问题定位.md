# 主题类型字段问题定位

## 🐛 问题现象

**显示内容**: `subject_type_4` （显示的是代码，而不是中文标签）

**预期显示**: 应该显示中文标签，如"医疗质量安全"

---

## 📍 问题定位

### 前端代码位置

**文件**: `src/views/committee/interview/Group.vue`

**显示位置**: 第235-237行
```vue
<el-descriptions-item label="主题类型">
  {{ currentDetail.activityInfo.subjectTypeLabel || '未填写' }}
</el-descriptions-item>
```

**字段路径**: `currentDetail.activityInfo.subjectTypeLabel`

### 数据来源

**API调用位置**: 第520行
```javascript
const res = await getRegistration(id)
```

**API定义文件**: `src/api/registration.js` 第129-134行
```javascript
export function getRegistration(id) {
  return request({
    url: `/registrations/${id}`,
    method: 'get'
  })
}
```

---

## 🎯 后端API信息

### API地址
```
GET /api/registrations/{id}
```

### 问题字段
```
响应路径: data.activityInfo.subjectTypeLabel
```

### 当前返回（错误）
```json
{
  "success": true,
  "data": {
    "activityInfo": {
      "subjectTypeCode": "subject_type_4",
      "subjectTypeLabel": null  // ← 问题：Label 为空或不存在
    }
  }
}
```

### 期望返回（正确）
```json
{
  "success": true,
  "data": {
    "activityInfo": {
      "subjectTypeCode": "subject_type_4",
      "subjectTypeLabel": "医疗质量安全"  // ← 应该返回中文标签
    }
  }
}
```

---

## 📋 需要后端修复的接口

| 项目 | 值 |
|------|-----|
| **API** | `GET /api/registrations/{id}` |
| **问题字段** | `data.activityInfo.subjectTypeLabel` |
| **当前状态** | 返回 `null` 或不存在 |
| **期望状态** | 返回中文标签（如"医疗质量安全"） |
| **字段类型** | String |

---

## 🔍 相关字段

在同一个 API 响应的 `activityInfo` 对象中，还有其他类似的 Label 字段：

```json
{
  "activityInfo": {
    "subjectTypeCode": "subject_type_4",
    "subjectTypeLabel": "医疗质量安全",  // ← 主题类型标签
    
    "methodCode": "method_1",
    "methodLabel": "PDCA循环法",         // ← 品管工具标签
    
    "experienceImproveCode": "experience_1",
    "experienceImproveLabel": "改善就医环境", // ← 改善就医环境标签
    
    "qualityTopicCode": "quality_1",
    "qualityTopicLabel": "医疗质量安全"     // ← 医疗质量主题标签
  }
}
```

**建议后端检查**: 所有这些 Label 字段是否都正确返回了中文标签。

---

## 🧪 验证方法

### 方法1: 浏览器开发者工具
1. 打开面谈分组页面
2. 点击某个项目的"详情"按钮
3. 按 F12 打开开发者工具 → Network 标签
4. 找到 `/api/registrations/{id}` 请求
5. 查看 Response 中的 `data.activityInfo.subjectTypeLabel` 字段

### 方法2: 使用Postman或curl
```bash
curl http://localhost:6031/api/registrations/{id} \
  -H "Authorization: Bearer {token}"
```

### 方法3: 查看控制台日志
前端已添加日志输出（第524行）：
```javascript
console.log('✅ 详情加载成功:', res.data)
```

打开浏览器控制台，展开 `activityInfo` 对象，检查 `subjectTypeLabel` 的值。

---

## 💡 后端可能的问题

### 1. 字段映射缺失
后端在查询数据库时，可能只返回了 `subjectTypeCode`，没有关联查询对应的字典表来获取 `subjectTypeLabel`。

### 2. 字典数据缺失
数据库中的字典表可能缺少 `subject_type_4` 对应的标签数据。

### 3. DTO 映射问题
后端的 DTO（数据传输对象）可能没有包含 `subjectTypeLabel` 字段。

---

## 📊 完整的字段对照表

| 字段名称 | 字段类型 | 当前值示例 | 期望值示例 |
|---------|---------|----------|-----------|
| `subjectTypeCode` | String | `subject_type_4` | `subject_type_4` |
| `subjectTypeLabel` | String | `null` ❌ | `医疗质量安全` ✅ |
| `methodCode` | String | `method_1` | `method_1` |
| `methodLabel` | String | 需检查 | `PDCA循环法` |
| `experienceImproveCode` | String | `experience_1` | `experience_1` |
| `experienceImproveLabel` | String | 需检查 | `改善就医环境` |
| `qualityTopicCode` | String | `quality_1` | `quality_1` |
| `qualityTopicLabel` | String | 需检查 | `医疗质量安全` |

---

## ✅ 修复确认清单

请后端确认以下内容：

- [ ] `GET /api/registrations/{id}` 接口返回的 `activityInfo` 对象中包含 `subjectTypeLabel` 字段
- [ ] `subjectTypeLabel` 字段的值是中文标签，不是代码
- [ ] 其他类似的 Label 字段（`methodLabel`、`experienceImproveLabel`、`qualityTopicLabel`）也都正确返回
- [ ] 字典表中有完整的代码和标签对照数据

---

**报告时间**: 2026-02-26  
**前端代码**: 无需修改（已正确使用 Label 字段）  
**后端修复**: 需要在 `GET /api/registrations/{id}` 接口中返回 `subjectTypeLabel` 字段
