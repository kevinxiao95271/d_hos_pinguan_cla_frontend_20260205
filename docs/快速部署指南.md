# 浙江省品管大赛前端部署指南

## 服务器信息

- 服务器IP: 81.71.44.180
- 用户名: root
- 密码: Yiguo9527_
- 前端端口: 6039
- 后端端口: 6031

## 方式一：使用批处理文件（推荐）

1. **构建项目**
   ```bash
   npm run build
   ```

2. **双击运行 `一键部署.bat`**
   - 输入密码：`Yiguo9527_`
   - 等待文件上传完成

3. **登录服务器配置nginx**
   ```bash
   ssh root@81.71.44.180
   # 密码: Yiguo9527_
   ```

4. **复制 `nginx_config.txt` 内容到服务器执行**
   （文件内容会在bat脚本运行时显示）

5. **重启nginx**
   ```bash
   nginx -t && systemctl restart nginx
   ```

6. **验证**
   - 浏览器访问: http://81.71.44.180:6039
   - 检查API: http://81.71.44.180:6039/api/

## 方式二：使用WinSCP手动上传

### 步骤1: 打开WinSCP

1. 文件协议: SCP
2. 主机名: 81.71.44.180
3. 端口号: 22
4. 用户名: root
5. 密码: Yiguo9527_

### 步骤2: 上传文件

1. 在右侧面板进入 `/data/` 目录
2. 如果没有 `pinguan_frontend` 文件夹，右键创建
3. 进入 `pinguan_frontend` 文件夹
4. 将本地 `dist` 文件夹内的所有文件拖入（不要上传dist文件夹本身）

### 步骤3: 配置nginx

点击WinSCP顶部菜单"命令 > 打开终端"，或使用PuTTY/MobaXterm连接服务器

```bash
# 1. 创建nginx配置
cat > /etc/nginx/conf.d/pinguan.conf << 'EOF'
server {
    listen 6039;
    server_name _;

    root /data/pinguan_frontend;
    index index.html;

    # Gzip压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript 
               application/x-javascript application/xml+rss 
               application/javascript application/json;

    # 前端静态文件
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API代理到后端（端口6031）
    location /api/ {
        proxy_pass http://localhost:6031/api/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
EOF

# 2. 测试配置
nginx -t

# 3. 重启nginx
systemctl restart nginx

# 4. 检查状态
systemctl status nginx
netstat -tuln | grep 6039

# 5. 验证后端端口
netstat -tuln | grep 6031
```

### 步骤4: 验证部署

浏览器访问:
- 前端: http://81.71.44.180:6039
- API测试: http://81.71.44.180:6039/api/dictionary

## 常见问题

### Q1: scp命令不存在
**解决**: 安装OpenSSH客户端
- Windows 10/11: 设置 > 应用 > 可选功能 > 添加功能 > OpenSSH客户端

### Q2: nginx配置错误
**检查**:
```bash
nginx -t
cat /etc/nginx/conf.d/pinguan.conf
```

### Q3: 端口被占用
**检查**:
```bash
netstat -tuln | grep 6039
netstat -tuln | grep 6031  # 后端端口
```

### Q4: 文件权限问题
**修复**:
```bash
chmod -R 755 /data/pinguan_frontend
chown -R nginx:nginx /data/pinguan_frontend
```

### Q5: 后端API不通
**检查**:
```bash
# 检查后端服务是否运行在6031端口
netstat -tuln | grep 6031
ps aux | grep java

# 测试后端直接访问
curl http://localhost:6031/api/dictionary
```

## 重新部署

如果需要更新前端:

```bash
# 1. 本地构建
npm run build

# 2. 上传文件（覆盖）
scp -r dist/* root@81.71.44.180:/data/pinguan_frontend/

# 3. 清除nginx缓存（如果有）
ssh root@81.71.44.180 "nginx -s reload"
```

## 端口说明

- **前端端口 6039**: 用户访问的Web界面端口
- **后端端口 6031**: Java API服务端口（内部通过nginx代理）
- **nginx**: 监听6039端口，将 `/api/` 请求代理到 `localhost:6031`

## 部署清单

- [x] 构建项目 `npm run build`
- [x] 上传 dist 文件到服务器 `/data/pinguan_frontend/`
- [x] 创建 nginx 配置文件 `/etc/nginx/conf.d/pinguan.conf`
- [x] 配置API代理到后端端口 6031
- [x] 重启 nginx
- [x] 测试前端访问 http://81.71.44.180:6039
- [x] 测试API访问 http://81.71.44.180:6039/api/
