# 自动分组每组人数调整

## 📝 修改说明

**修改时间**: 2026-02-26  
**修改内容**: 将自动分组逻辑的每组人数从 **10人** 调整为 **25人**

---

## 🔧 修改的文件

### 1. 书审分组页面

**文件**: `src/views/committee/book/Registration.vue`

**修改位置**: 第756-776行 `autoGroup()` 函数

**修改前**:
```javascript
const autoGroup = async () => {
  try {
    await ElMessageBox.confirm(
      '确定要自动分配分组吗？将按每组10人自动分配到A组系列（A1、A2、A3...）',  // ← 10人
      '提示',
      {
        type: 'warning',
        confirmButtonText: '确定',
        cancelButtonText: '取消'
      }
    )
    
    const res = await autoGroupRegistrations({
      competitionId: filters.competitionId,
      groupPrefix: 'A',
      groupSize: 10      // ← 10人
    })
```

**修改后**:
```javascript
const autoGroup = async () => {
  try {
    await ElMessageBox.confirm(
      '确定要自动分配分组吗？将按每组25人自动分配到A组系列（A1、A2、A3...）',  // ← 25人
      '提示',
      {
        type: 'warning',
        confirmButtonText: '确定',
        cancelButtonText: '取消'
      }
    )
    
    const res = await autoGroupRegistrations({
      competitionId: filters.competitionId,
      groupPrefix: 'A',
      groupSize: 25      // ← 25人
    })
```

**变更**:
- ✅ 提示文字：`每组10人` → `每组25人`
- ✅ API参数：`groupSize: 10` → `groupSize: 25`

---

### 2. 面谈分组页面

**文件**: `src/views/committee/interview/Group.vue`

**修改位置**: 第559-608行 `autoGroupInterview()` 函数

**修改前**:
```javascript
const autoGroupInterview = async () => {
  try {
    await ElMessageBox.prompt('请输入每组人数', '自动分组', {
      confirmButtonText: '确定',
      cancelButtonText: '取消',
      inputPattern: /^[1-9]\d*$/,
      inputErrorMessage: '请输入有效的数字',
      inputValue: '6'      // ← 默认6人
    }).then(async ({ value }) => {
```

**修改后**:
```javascript
const autoGroupInterview = async () => {
  try {
    await ElMessageBox.prompt('请输入每组人数', '自动分组', {
      confirmButtonText: '确定',
      cancelButtonText: '取消',
      inputPattern: /^[1-9]\d*$/,
      inputErrorMessage: '请输入有效的数字',
      inputValue: '25'     // ← 默认25人
    }).then(async ({ value }) => {
```

**变更**:
- ✅ 默认值：`inputValue: '6'` → `inputValue: '25'`

**说明**: 面谈分组支持手动输入人数，这里修改的是默认值。用户仍可以输入其他数字。

---

### 3. 赛事详情页面

**文件**: `src/views/committee/CompetitionDetail.vue`

**修改位置**: 第437-447行 `autoGroup()` 函数

**修改前**:
```javascript
const autoGroup = async () => {
  try {
    await autoGroupRegistrations({
      competitionId: competitionId.value
      // 未指定 groupSize，使用后端默认值
    })
    ElMessage.success('自动分组成功')
    loadRegistrations()
  } catch (error) {
    console.error('自动分组失败:', error)
  }
}
```

**修改后**:
```javascript
const autoGroup = async () => {
  try {
    await autoGroupRegistrations({
      competitionId: competitionId.value,
      groupPrefix: 'A',
      groupSize: 25        // ← 明确指定25人
    })
    ElMessage.success('自动分组成功')
    loadRegistrations()
  } catch (error) {
    console.error('自动分组失败:', error)
  }
}
```

**变更**:
- ✅ 新增参数：`groupPrefix: 'A'`
- ✅ 新增参数：`groupSize: 25`

**说明**: 原本没有指定分组参数，现在明确指定为25人一组。

---

## 📊 修改总览

| 页面 | 文件 | 原每组人数 | 新每组人数 | 修改方式 |
|------|------|-----------|-----------|---------|
| 书审分组 | `Registration.vue` | 10人 | 25人 | 固定值 |
| 面谈分组 | `Group.vue` | 6人（默认） | 25人（默认） | 默认值（可修改） |
| 赛事详情 | `CompetitionDetail.vue` | 未指定 | 25人 | 新增参数 |

---

## 🎯 影响范围

### 1. 书审阶段自动分组
- **位置**: 书审分组页面 → 自动分组按钮
- **行为**: 点击后按每组25人自动分配到A组系列（A1、A2、A3...）
- **示例**: 
  - 75个项目 → 分成A1、A2、A3（每组25人）
  - 80个项目 → 分成A1、A2、A3、A4（前3组25人，最后一组5人）

### 2. 面谈阶段自动分组
- **位置**: 面谈分组页面 → 自动分组按钮
- **行为**: 弹出输入框，默认值为25人（用户可修改）
- **灵活性**: 用户可以根据实际情况输入其他人数

### 3. 赛事详情页自动分组
- **位置**: 赛事详情页面 → 自动分组按钮
- **行为**: 按每组25人自动分配到A组系列

---

## ⚠️ 注意事项

### 1. 前端修改，后端无需改动
- 后端API支持接收 `groupSize` 参数
- 前端只是修改了传递给后端的参数值
- 后端逻辑无需调整

### 2. 不影响已有分组
- 自动分组只对"未分组"的项目生效
- 已经分组的项目不受影响
- 可以手动调整分组

### 3. 分组命名规则
- 分组前缀：`A`
- 分组编号：从1开始递增（A1、A2、A3...）
- 最后一组可能不满25人（如果总数不是25的倍数）

### 4. 面谈分组的灵活性
- 面谈分组保持输入框方式
- 默认值改为25，但用户可以根据需要输入其他值
- 支持任意正整数

---

## 🧪 测试建议

### 测试场景1: 书审分组
1. 进入书审分组页面
2. 准备一些未分组的项目（如50个）
3. 点击"自动分组"按钮
4. 确认提示语显示"每组25人"
5. 确认后查看分组结果
6. **预期**: 50个项目分成A1（25人）、A2（25人）

### 测试场景2: 面谈分组
1. 进入面谈分组页面
2. 准备一些未分组的项目
3. 点击"自动分组"按钮
4. **预期**: 输入框默认值为"25"
5. 可以修改为其他值（如20）测试灵活性

### 测试场景3: 不满25人
1. 准备40个未分组项目
2. 执行自动分组
3. **预期**: 分成A1（25人）、A2（15人）

### 测试场景4: 恰好25的倍数
1. 准备75个未分组项目
2. 执行自动分组
3. **预期**: 分成A1、A2、A3（每组恰好25人）

---

## 📋 API说明

### 自动分组API

**接口**: `POST /api/admin/registrations/auto-group`

**请求参数**:
```json
{
  "competitionId": 1,
  "groupPrefix": "A",
  "groupSize": 25
}
```

**参数说明**:
- `competitionId`: 赛事ID
- `groupPrefix`: 分组前缀（如"A"）
- `groupSize`: 每组人数（现在固定为25）

**响应**:
```json
{
  "success": true,
  "message": "自动分组成功"
}
```

---

## 📝 后续建议

### 1. 配置化
如果将来需要频繁调整每组人数，建议：
- 将每组人数配置到系统设置中
- 管理员可以在设置页面修改
- 前端从配置中读取，无需改代码

### 2. 动态输入
可以考虑将书审分组也改为输入框方式：
- 像面谈分组一样，支持用户输入
- 提供一个推荐值（如25）
- 更加灵活

### 3. 分组策略
可以考虑更智能的分组策略：
- 按地区分组
- 按机构级别分组
- 按品管工具分组
- 避免同一机构在同一组

---

## ✅ 修改完成

- ✅ 书审分组：每组25人
- ✅ 面谈分组：默认25人（可修改）
- ✅ 赛事详情：每组25人
- ✅ 无linter错误
- ✅ 逻辑清晰，易于维护

---

**修改完成时间**: 2026-02-26  
**修改人**: AI助手  
**测试状态**: 待测试
