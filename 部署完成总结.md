# 🎉 部署准备工作已完成

## ✅ 已完成的步骤

### 1. Git操作
- ✅ 代码已提交到本地仓库
  - 提交信息：修复登录页面UI和面谈分组详情显示问题
  - 修改文件：14个文件
- ✅ 代码已推送到远端
  - 远端仓库：github.com/kevinxiao95271/d_hos_pinguan_cla_frontend_20260205.git
  - 分支：`web-20260206`
  - 提交哈希：a971031

### 2. 本地编译
- ✅ 执行命令：`npm run build`
- ✅ 编译时间：39.58秒
- ✅ 编译产物：`dist/` 目录
- ✅ 主要文件：
  - index.html (0.47 kB)
  - assets/index-DlsYKFKp.js (1,211.50 kB)
  - assets/index-BhiroXkb.css (349.95 kB)
  - 其他资源文件

### 3. 打包编译产物
- ✅ 已打包：`dist.zip` (约47MB)
- ✅ 位置：`D:\AiCode\cursor\d_hos_pinguan_cla_frontend_20260205\dist.zip`

### 4. 部署文档
- ✅ 创建：`DEPLOY.md` - 详细部署说明
- ✅ 创建：`deploy.sh` - Linux/Mac部署脚本
- ✅ 创建：`deploy.ps1` - Windows PowerShell部署脚本

---

## 🚀 下一步：部署到服务器

**服务器信息**：
- 🖥️ IP: `81.71.44.180`
- 👤 用户: `root`
- 🔑 密码: `Yiguo9527_`
- 📂 目标路径: `/data/frontend/dist`

---

## 📋 快速部署步骤（推荐方式）

### 方式A：使用WinSCP（最简单）

1. **下载WinSCP**: https://winscp.net/eng/download.php

2. **连接服务器**：
   ```
   协议: SFTP
   主机: 81.71.44.180
   端口: 22
   用户名: root
   密码: Yiguo9527_
   ```

3. **上传文件**：
   - 服务器端导航到 `/data/` 
   - 如果没有 `frontend` 目录，右键创建
   - 将本地 `dist` 目录拖拽上传到 `/data/frontend/`

4. **完成**！访问 `http://81.71.44.180` 验证

---

### 方式B：使用命令行（适合熟悉命令行的用户）

#### 在Git Bash或PowerShell中执行：

```bash
# 进入项目目录
cd D:\AiCode\cursor\d_hos_pinguan_cla_frontend_20260205

# 上传压缩包到服务器
scp dist.zip root@81.71.44.180:/tmp/
# 输入密码: Yiguo9527_

# 连接到服务器
ssh root@81.71.44.180
# 输入密码: Yiguo9527_
```

**在服务器上执行（SSH连接后）**：

```bash
# 创建目录
mkdir -p /data/frontend

# 备份旧版本（如果存在）
if [ -d "/data/frontend/dist" ]; then
    mv /data/frontend/dist /data/frontend/dist.backup.$(date +%Y%m%d_%H%M%S)
    echo "✅ 已备份旧版本"
fi

# 安装unzip（如果没有）
yum install -y unzip || apt-get install -y unzip

# 解压新版本
cd /data/frontend
unzip -o /tmp/dist.zip -d dist/

# 清理
rm -f /tmp/dist.zip

# 验证
echo "📁 部署文件列表："
ls -lh /data/frontend/dist/

# 退出
exit
```

---

## 🔍 部署后验证

### 1. 检查文件是否存在

```bash
ssh root@81.71.44.180
ls -la /data/frontend/dist/
```

应该看到：
```
total 12
drwxr-xr-x  3 root root 4096 Feb  8 15:30 .
drwxr-xr-x  3 root root 4096 Feb  8 15:30 ..
drwxr-xr-x  2 root root 4096 Feb  8 15:30 assets
-rw-r--r--  1 root root  470 Feb  8 15:30 index.html
```

### 2. 配置Web服务器

如果使用**Nginx**，配置文件 `/etc/nginx/conf.d/frontend.conf`：

```nginx
server {
    listen 80;
    server_name 81.71.44.180;

    root /data/frontend/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # 缓存静态资源
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

重启Nginx：
```bash
nginx -t          # 检查配置
nginx -s reload   # 重新加载
# 或
systemctl restart nginx
```

如果使用**Apache**，配置文件 `/etc/httpd/conf.d/frontend.conf`：

```apache
<VirtualHost *:80>
    ServerName 81.71.44.180
    DocumentRoot /data/frontend/dist

    <Directory /data/frontend/dist>
        Options -Indexes +FollowSymLinks
        AllowOverride All
        Require all granted

        # Vue Router History模式支持
        RewriteEngine On
        RewriteBase /
        RewriteRule ^index\.html$ - [L]
        RewriteCond %{REQUEST_FILENAME} !-f
        RewriteCond %{REQUEST_FILENAME} !-d
        RewriteRule . /index.html [L]
    </Directory>
</VirtualHost>
```

重启Apache：
```bash
apachectl configtest  # 检查配置
systemctl restart httpd
```

### 3. 测试访问

浏览器打开：**http://81.71.44.180**

应该看到**浙江省品管大赛管理系统**登录页面

---

## 📁 本地文件位置

| 项目 | 路径 |
|------|------|
| 源代码 | `D:\AiCode\cursor\d_hos_pinguan_cla_frontend_20260205` |
| 编译产物 | `D:\AiCode\cursor\d_hos_pinguan_cla_frontend_20260205\dist` |
| 压缩包 | `D:\AiCode\cursor\d_hos_pinguan_cla_frontend_20260205\dist.zip` |
| 部署文档 | `D:\AiCode\cursor\d_hos_pinguan_cla_frontend_20260205\DEPLOY.md` |

---

## 🔄 后续更新流程

当需要再次部署时：

```bash
# 1. 更新代码
git pull origin web-20260206

# 2. 重新编译
npm run build

# 3. 打包
powershell -Command "Compress-Archive -Path 'dist\*' -DestinationPath 'dist.zip' -Force"

# 4. 上传部署（重复上述部署步骤）
```

---

## ⚠️ 常见问题

### Q1: 访问显示403 Forbidden
**解决**：检查文件权限
```bash
chmod -R 755 /data/frontend/dist
```

### Q2: 刷新页面显示404
**解决**：配置Web服务器的History模式支持（见上面配置示例）

### Q3: 静态资源加载失败
**解决**：检查Nginx/Apache配置中的root路径是否正确

### Q4: 无法访问服务器
**解决**：
1. 检查防火墙：`firewall-cmd --add-port=80/tcp --permanent`
2. 检查安全组：云服务器需要开放80端口
3. 检查Web服务器状态：`systemctl status nginx`

---

## 📞 需要帮助？

查看详细文档：`DEPLOY.md`

---

**🎉 所有准备工作已完成，现在可以开始部署了！**

推荐使用 **WinSCP** 进行部署，简单直观！
